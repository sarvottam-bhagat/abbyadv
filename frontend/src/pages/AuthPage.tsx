import { useState } from 'react';
import type { FormEvent } from 'react';
import { useNavigate } from 'react-router-dom';
import { supabase } from '../lib/supabase';
export default function AuthPage() { const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const [mode,setMode]=useState<'signin'|'signup'>('signin'); const [error,setError]=useState(''); const navigate=useNavigate();
  async function submit(e: FormEvent) { e.preventDefault(); setError(''); if (!supabase) { navigate('/'); return; } const result = mode==='signin' ? await supabase.auth.signInWithPassword({email,password}) : await supabase.auth.signUp({email,password}); if (result.error) setError(result.error.message); else navigate('/'); }
  return <div className="auth-page"><div className="auth-art"><div className="brand"><div className="brand-mark">A</div><strong>AbbyAdv</strong></div><div><p className="eyebrow">A calmer way to practise law</p><h1>Keep every matter<br/><em>in view.</em></h1><p>Client context, documents, research and drafting—together in one considered workspace.</p></div><span className="auth-quote">“The best legal work starts with a clear picture.”</span></div><form className="auth-card" onSubmit={submit}><div className="mobile-brand brand"><div className="brand-mark">A</div><strong>AbbyAdv</strong></div><p className="eyebrow">Welcome back</p><h2>{mode==='signin'?'Sign in to your workspace':'Create your workspace'}</h2><p className="muted">{mode==='signin'?'Continue where your matters left off.':'Set up your private legal workspace.'}</p><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required placeholder="you@example.com"/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required minLength={6} placeholder="••••••••"/></label>{error&&<div className="error-box">{error}</div>}<button className="primary-button full" type="submit">{mode==='signin'?'Sign in':'Create account'}</button><button type="button" className="text-button" onClick={()=>setMode(mode==='signin'?'signup':'signin')}>{mode==='signin'?'New to AbbyAdv? Create an account':'Already have an account? Sign in'}</button></form></div>;
}
