import { ArrowUpRight, FileText, Plus, Search, Sparkles } from 'lucide-react';
export default function GenericPage({title,kicker,description}:{title:string;kicker:string;description:string}){return <div><div className="page-heading"><div><p className="eyebrow">{kicker}</p><h1>{title}</h1><p className="muted">{description}</p></div><button className="primary-button"><Plus size={17}/> New</button></div><div className="empty-card large-empty"><div className="empty-icon indigo"><Sparkles size={25}/></div><h2>Your {title.toLowerCase()} will live here</h2><p>We have connected the workspace foundation. Create a new item or use AbbyAdv chat to get started.</p><button className="secondary-button"><Search size={16}/> Explore workspace <ArrowUpRight size={15}/></button></div></div>}
